import { InsideBoundariesDirective } from './inside-boundaries.directive';

describe('InsideBoundariesDirective', () => {
  it('should create an instance', () => {
    const directive = new InsideBoundariesDirective();
    expect(directive).toBeTruthy();
  });
});
