import { MutationObserverDirective } from './mutation-observer.directive';

describe('MutationObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new MutationObserverDirective();
    expect(directive).toBeTruthy();
  });
});
