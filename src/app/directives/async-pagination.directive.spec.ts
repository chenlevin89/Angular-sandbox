import { AsyncPaginationDirective } from './async-pagination.directive';

describe('AsyncPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
