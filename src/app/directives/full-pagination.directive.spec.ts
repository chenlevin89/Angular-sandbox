import { FullPaginationDirective } from './full-pagination.directive';

describe('FullPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new FullPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
